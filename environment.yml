name: transformer
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pytorch>=2.0.0
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  - numpy>=1.24.0
  - pyyaml>=6.0
  - matplotlib>=3.7.0
  - pytest>=7.0.0
  - tqdm>=4.65.0
  - pip
  - pip:
      - datasets>=2.14.0
      - tokenizers>=0.15.0
      - wandb>=0.15.0
      - evaluate>=0.4.0
